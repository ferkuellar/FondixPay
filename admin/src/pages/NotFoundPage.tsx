import { EmptyState } from "../components/EmptyState";

export function NotFoundPage() {
  return <EmptyState title="Vista no encontrada" message="Usa la navegacion admin para volver a un modulo permitido." />;
}
