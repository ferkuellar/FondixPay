# Claude Adapter

Claude must read `AGENTS.md` first and treat it as the project operating source.

Claude must not redefine scope, create new product direction, or treat FondixPay as a blank project. Work must start from the active sprint under `planning/sprints/` and preserve the existing mobile and backend implementation unless the sprint explicitly approves implementation changes.
